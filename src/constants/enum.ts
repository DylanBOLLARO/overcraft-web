export enum VIEW {
    BUILD,
    DRAFT,
    UPDATE,
}

export enum STYLE_VIEW {
    LINE,
    SQUARE,
}

export enum pagePath {
    // nav
    HOME = "/",
    DOCUMENTATION = "/documentation",
    DASHBOARD = "/dashboard/builds",
    SIGNIN = "/login",

    UPDATE = "/update",
}