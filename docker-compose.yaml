version: '3'

services:
  nextjs:
    build:
      context: .
    ports:
      - 3000:3000
    container_name: nextjs

  nginx:
    build: ./nginx/.
    ports:
      - "80:80"
    depends_on:
      - nextjs
    container_name: nginx
